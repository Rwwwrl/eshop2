apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway
spec:
  replicas: 3
  template:
    spec:
      containers:
        - name: api-gateway
          image: ${IMAGE_REGISTRY}/api-gateway:${IMAGE_TAG}
          imagePullPolicy: Always

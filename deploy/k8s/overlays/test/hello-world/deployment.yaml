apiVersion: apps/v1
kind: Deployment
metadata:
  name: hello-world-service
spec:
  replicas: 3
  template:
    spec:
      containers:
        - name: hello-world-service
          image: ${IMAGE_REGISTRY}/hello-world-service:${IMAGE_TAG}
          imagePullPolicy: Always

apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: hello-world-secrets
spec:
  refreshInterval: 1h
  refreshPolicy: OnChange
  secretStoreRef:
    name: gcp-cluster-store
    kind: ClusterSecretStore
  target:
    name: hello-world-secrets
    creationPolicy: Owner
  # NOTE @sosov: Placeholder to satisfy the admission webhook — replace with real secrets when needed.
  data:
    - secretKey: PLACEHOLDER
      remoteRef:
        key: placeholder
